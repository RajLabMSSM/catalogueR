# Check MAF

Check Minor Allele Frequency (MAF) column is prepared for running coloc.
Must be numeric, \<1, \>0, and not contain NAs.

## Usage

``` r
check_maf(gwas_shared, eqtl_shared, verbose = TRUE)
```

## Arguments

- verbose:

  Print messages.
